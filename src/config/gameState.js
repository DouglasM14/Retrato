const gameState = function () {
    const playersOrder = []
    const actualPlayer = 0
    const readerPlayer = 0

    return {
        playersOrder,
        actualPlayer,
        readerPlayer
    }
}();